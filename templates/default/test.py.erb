#!/usr/bin/python

import socket
import sys

IP=sys.argv[1]
PORT=sys.argv[2]
BUFFER_SIZE = 1024
print "IP="+IP+" PORT="+PORT
MESSAGE = "--TEST LINE---"

try:
 sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
 sock.settimeout(2)
 sock.connect((IP, int(PORT)))
 data=sock.recv(1024)
 print data
 print "Success"
 sock.close()
except Exception, e:
 print "Failure "+str(e)
